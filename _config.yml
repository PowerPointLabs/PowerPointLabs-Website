name: PowerPointLabs
markdown: redcarpet
highlighter: pygments
# baseurl: /~pptlabs
collections:
  docs:
    output: true

gems: ['jekyll-less']
exclude: ['vendor', 'Gemfile', 'Gemfile.lock', 'Vagrantfile', 'README.md', '.gitignore', '.vagrant']
